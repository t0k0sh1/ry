[English](../../reference/errors.md) | [日本語](../../ja/reference/errors.md) | [简体中文](errors.md)

# 错误参考

## 错误格式

ry 以 Rust 风格的丰富格式显示编译错误，显示错误的确切位置及源代码上下文：

```
error: cannot reassign @const variable: x
  --> main.ry:5:1
  |
5 | x = 10
  | ^ cannot reassign @const variable: x
```

每条错误消息包含：
- **错误等级和消息** (`error: ...`)
- **文件位置** (`--> 文件:行:列`)
- **源代码行** (相关代码)
- **插入符号指示器** (`^`) 指向确切的列位置

## 编译错误

| 错误内容 | 原因 | 示例 |
|-----------|------|-----|
| 使用未声明的变量 | 引用了未声明的变量 | `print(x)`（`x` 未声明） |
| 对 @const 变量重新赋值 | 对以 `@const` 声明的变量重新赋值 | `@const x = 1` -> `x = 2` |
| 重新声明同名变量 | 在同一作用域重新声明同名变量 | `x = 1` -> 同名再声明 |
| 变量类型变更赋值 | 对变量赋值不同类型的值 | `x = 1` -> `x = 3.14` |
| 类型标注不一致 | 声明时的类型与赋值的类型不同 | `x: int = 3.14` |
| 重载返回类型冲突 | 定义了参数类型相同但仅返回类型不同的重载 | 参数 `(int, int)` 但返回类型分别为 `int` 和 `float` 的两个函数 |
| 重载解析失败 | 不存在与参数类型匹配的重载 | `function add(a: int, b: int)` 却调用 `add(1.0, 2.0)` |
| 对位运算传入 float | 对 `&`、`\|`、`^`、`~`、`<<`、`>>` 传入 `float` 类型 | `3.14 & 1` |
| 空列表 | 无法从空列表字面值 `[]` 推断类型 | `xs = []` |
| 空映射 | 无法从空映射字面值 `{}` 推断类型 | `m = {}` |
| 元组超出范围的索引 | 访问元组中不存在的索引 | `t = (1, 2)` -> `t.2` |
| 在循环外使用 break/continue | 在 `for`/`while` 循环外使用 `break` 或 `continue` | 在函数顶层使用 `break` |
| 在块内导入模块 | 在函数或条件块内使用 `from` 语句 | 在函数内使用 `from math` |
| 循环导入 | 模块之间互相导入 | `a.ry` 导入 `b.ry`，`b.ry` 导入 `a.ry` |
| 相同字段名重复 | 在结构体内定义了两次相同的字段名 | 在 `type T: x: int` 中定义了两个 `x` |
| 非穷尽 match | `match` 未覆盖所有模式 | enum 的部分变体未覆盖、Option 缺少 `None`、Result 缺少 `Ok`/`Err`、字面值缺少 `_` |
| `?` 用于非 Result 类型 | 对非 `Result` 类型的表达式使用 `?` | `x = 42` -> `x?` |
| `?` 用于非 Result 函数 | 在不返回 `Result` 的函数中使用 `?` | `function foo() -> int:` -> `bar()?` |

### 编译错误示例

```python
# 对 @const 变量重新赋值
@const
x = 10
x = 20   # 错误

# 类型变更赋值
n = 1
n = "hello"   # 错误：对 int 类型赋值 str 类型

# 空列表
xs = []   # 错误：无法推断类型

# 在循环外使用 break
break   # 错误：在循环外

# 在块内导入
function foo():
    from math   # 错误：仅能在顶层导入

# 相同字段名重复
record Bad:
    x: int
    x: float   # 错误：x 重复
```

---

## 运行时错误

| 错误内容 | 原因 | 示例 |
|-----------|------|-----|
| 列表超出范围的访问 | 列表的索引超出范围 | `xs = [1, 2, 3]` -> `xs[5]` |
| 映射不存在的键访问 | 引用映射中不存在的键 | `m = {"a": 1}` -> `m["b"]` |
| 契约违反 | `require`、`ensure` 或 `invariant` 条件评估为 false | 参阅 [契约式设计](contracts.md) |

所有运行时错误都会以 `exit(1)` 终止程序。

### 运行时错误示例

```python
# 列表超出范围的访问
xs = [1, 2, 3]
print(xs[10])   # 运行时错误：exit(1)

# 映射不存在的键访问
m = {"a": 1}
print(m["z"])   # 运行时错误：exit(1)
```
