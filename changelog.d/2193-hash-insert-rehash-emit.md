### refactor(codegen): migrate Map/Set insert+rehash capability + emitMapMergeCore via crates/emit primitives (#2193)

Follow-on sweep settling #2101's "Settle ≠ unlock" carve-out for hash insert+rehash. The two shared helpers `emitBucketInsertAndRehashCheck` and `coerceHashKey` (`src/codegen_builtin.cpp`) and the 150-line `emitMapMergeCore` scaffold (`src/codegen_call_collection.cpp`) drop every `builder_.Create*` in favor of the existing `ry_emit_*` primitive vocabulary — **no new boundary entry, no new abi shell, no new C++ wrapper**. The 10 implicit callers (Set `add` / Set literal / Set `union` / `intersection` / `difference` / `symmetricDifference` ×2 / Map index assign / Map `merge` map1 + map2) inherit the migration through the two shared helpers. `__ry_hash_{i64,str,f64}` / `__ry_ht_insert` (void return) / `__ry_ht_rehash_{i64,str,f64}` / `free` / `malloc` / `memcpy` route through `emitRuntimeCallDirect`; `__ry_ht_insert`'s void-return path extends the `emitRuntimeCallDirect("free", voidTy, ...)` precedent from `src/codegen_arc.cpp`. `coerceHashKey`'s lone `Set<bool>` / `Map<bool, V>` i1→i64 widening becomes `emitZExt` preserving the SSA name `"<prefix>_hash_zext"` byte-for-byte. Carve-outs (stay C++ — boundary doc tracks each separately): `loadMapHeader` / `loadSetHeader` (header-load #2093/#2101), `emitMapKeyLookup`'s linear-scan path (#2101), `propagateTypeMeta` / `setTypeMeta` / `propagateMeta` (#2100 ValueMetadata crossing), the ARC dispatch cluster (`emitCowRetainArcElements` / `retainArcValue` / `emitArcRelease` / `emitArcGetHeaderFromData` / `emitStrGetHeaderFromData`), and the header-write helpers `emitBucketInit` / `storeMapHeaderFields` / `emitArcAllocCollectionHeader`. The critical rehash-BB instruction order (`bc_cur` re-load → keys load → `__ry_ht_rehash_*` call → `old_buckets` load (AFTER the rehash call) → `free(old_buckets)` → `store new_buckets`) is preserved byte-for-byte as a structural UAF guard, mirroring the `weak_count` atomic/non-atomic (#1968) and `load_list_header` interleave (#2092) lessons. Pure refactor — ASLR-normalized `--emit-llvm-ir` diff is empty across `tests/filecheck/set_insert_rehash_{i64,str,f64}_parity.ry` + `set_insert_bool_zext_parity.ry` + `map_merge_rehash_parity.ry`, each forcing a `len*4 > bc*3` rehash trigger with non-constant function-parameter operands (#2092 coverage trap); `ry_tests` (2795) and `ry test -p` (207 spec files) stay green.
