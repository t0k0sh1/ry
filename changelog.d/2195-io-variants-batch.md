### refactor(codegen): migrate IO boundary variants (network listen + json anyTy_ slot) via crates/emit primitives (#2195)

v0.0.29 emission 拡張バッチの IO 境界 variants 2 件 (B2 / B3) を pilot B (#2100) で確立した境界経由 emission に揃える。`src/codegen_call_io.cpp::emitNetTcpListen` の `CreateICmpNE` / `CreateSelect` 2 op と、`src/codegen_call_json.cpp` の `emitJsonStringifyImpl` / `emitJsonLoad` / `emitJsonDumpFile` の anyTy_ alloca/store/load + status check op、および 2 箇所の `mod_->getOrInsertFunction` を `cg.emitICmpNE` / `emitSelect` / `emitAlloca` / `emitStore` / `emitLoad` / `getRuntimeFn` に置換 — いずれも `ry_emit_*` boundary を経由する既存 helper。`emitNetTcpListen` の `buildStaticError("listen failed")` runtime channel 不使用と `wrapPtrAsResult` helper 内側の `CreateICmpEQ` は behavior / 影響範囲の観点から本 batch では touch せず別 issue で triage。ASLR 正規化後の `--emit-llvm-ir` diff は coverage gate (#2026 false-pass 罠回避) 上で bit-exact 空。
