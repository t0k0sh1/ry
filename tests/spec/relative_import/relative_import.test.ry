from .helper import greet
from .mymath import add, sub

describe("relative import", fn():
    it("import from sibling module", fn():
        expect(greet()).to_eq("hello")
    )
    it("import from subdirectory package (multiple files)", fn():
        expect(add(2, 3)).to_eq(5)
        expect(sub(10, 4)).to_eq(6)
    )
)
