from . import add, sub

describe("relative import from current directory", function():
    it("imports symbols from multiple files in same directory", function():
        expect(add(2, 3)).to_eq(5)
        expect(sub(10, 4)).to_eq(6)
    )
)
