fn fetchData() -> str:
  return "real data"

fn compute(x: int) -> int:
  return x * 2

describe("mock", ():
  it("should replace function", ():
    mock("fetchData", () => "fake")
    expect(fetchData()).to_eq("fake")
  )
  it("should auto-restore after it block", ():
    expect(fetchData()).to_eq("real data")
  )
  it("should mock with arguments", ():
    mock("compute", (x: int) => x + 100)
    expect(compute(5)).to_eq(105)
  )
)

describe("verify", ():
  it("should count calls", ():
    mock("fetchData", () => "fake")
    fetchData()
    fetchData()
    expect(verify("fetchData")).to_eq(2)
  )
  it("should count zero calls", ():
    mock("fetchData", () => "fake")
    expect(verify("fetchData")).to_eq(0)
  )
)
