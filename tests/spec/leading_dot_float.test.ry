describe("leading-dot float literals", ():
  it(".5 equals 0.5", ():
    x = .5
    expect(x).to_eq(0.5)
  )
  it(".01 equals 0.01", ():
    x = .01
    expect(x).to_eq(0.01)
  )
  it("arithmetic with leading-dot floats", ():
    x = .5 + .25
    expect(x).to_eq(0.75)
  )
)
