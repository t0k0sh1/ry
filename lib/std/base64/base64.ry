# Base64 encoding and decoding

@native
fn encode(input: str) -> str

@native
fn decode(input: str) -> str

@native
fn encode_url_safe(input: str) -> str

@native
fn decode_url_safe(input: str) -> str
