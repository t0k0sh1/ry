# File I/O
from ry.io import readText, writeText

# Write a file
case writeText("/tmp/ry_test.txt", "hello from ry\nline 2\n"):
  Ok(_):
    print("written")
  Err(e):
    print(f"err: {e.message}")

# Read a file
case readText("/tmp/ry_test.txt"):
  Ok(text):
    print(f"read: {text}")
  Err(e):
    print(f"err: {e.message}")
