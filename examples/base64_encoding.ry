# base64モジュールによる文字列のエンコードとデコードを示す
from base64 import encode, decode

encoded = encode("Hello, Ry!")
print(encoded)

# decodeは不正入力を扱えるResultを返す
case decode(encoded):
  Ok(text):
    print(text)
  Err(e):
    print(e.message)
