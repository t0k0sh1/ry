# Shows string encoding and decoding with the base64 module
from ry.base64 import encode, decode

encoded = encode("Hello, Ry!")
print(encoded)

# decode returns a Result so invalid input can be handled
case decode(encoded):
  Ok(text):
    print(text)
  Err(e):
    print(e.message)
