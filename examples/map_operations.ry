# Shows safe Map lookup, insertion, and iteration
scores: Map<str, int> = {"alice": 90, "bob": 80}
scores["carol"] = 95

# get returns None for a missing key
print(get(scores, "alice"))
print(get(scores, "dave", 0))

for name, score in items(scores):
  print(f"{name}: {score}")
