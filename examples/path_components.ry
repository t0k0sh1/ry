# pathモジュールでpathの結合と構成要素の抽出を行う
from path import join, basename, dirname, ext

case join("/tmp", "reports", "summary.txt"):
  Ok(path):

    # path関数はNUL文字などの不正入力をErrとして返す
    print(basename(path)?)
    print(dirname(path)?)
    print(ext(path)?)
  Err(e):
    print(e.message)
