# Shows how to restore a JSON object into a typed record
from ry.json import load

record Person:
  name: str
  age: int

case load[Person]("{\"name\":\"Alice\",\"age\":30}"):
  Ok(person):
    print(f"{person.name} is {person.age}")
  Err(e):
    print(e.message)
