# Shows how List copy-on-write isolates changes made through aliases
original = [[1, 2], [3, 4]]
copy = original

# When rewriting nested elements, only the required path is copied
copy[0][1] = 99

print(f"original = {original}")
print(f"copy = {copy}")
