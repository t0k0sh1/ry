# JSON output using the stdlib json module
from ry.json import stringify

# Map<str, any> literal: values are automatically wrapped as any at literal positions
m: Map<str, any> = {"x": 1, "y": "hello"}
s = stringify(m)
print(f"stringified: {s}")
