# Iteratorによる遅延filter、map、takeパイプラインを示す
values = [1, 2, 3, 4, 5, 6]

# toListを呼ぶまで中間Listは作られない
result = toList(take(map(filter(iter(values), (n: int) => n % 2 == 0), (n: int) => n * n), 2))

print(result)
