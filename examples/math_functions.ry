# Shows constants, square root, rounding, and infinity checks from the math module
from ry.math import PI, INF, sqrt, round, isInf

radius = 3.0
area = PI * radius * radius

print(f"area = {round(area, 2)}")
print(f"sqrt(81) = {sqrt(81)}")
print(f"infinite constant = {isInf(INF)}")
