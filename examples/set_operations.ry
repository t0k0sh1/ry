# Shows Set deduplication and set operations
left = {1, 2, 3, 3}
right = {3, 4, 5}

print(f"left = {left}")
print(f"union = {union(left, right)}")
print(f"intersection = {intersection(left, right)}")
print(f"difference = {difference(left, right)}")
