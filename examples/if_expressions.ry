# Shows using if as both a statement and a value-producing expression
score = 85

if score >= 60:
  print("passed")
else:
  print("failed")

# An if expression returns the value of the selected branch
grade = if score >= 90 => "A" else if score >= 80 => "B" else "C"
print(grade)
