# ry - Claude Code 開発ガイドライン

## ビルド & テスト

```bash
cmake -B build -DLLVM_DIR=/usr/local/llvm/lib/cmake/llvm
cmake --build build
./build/ry_tests
```

## 作業完了前チェックリスト

タスクの完了前に、以下を必ず実行すること。

### 1. README.md 反映チェック

機能の**追加・変更・削除**を行った場合、README.md に反映すべき内容がないか確認する。

対象セクションと確認観点:

- **特徴** — 新機能やユーザー向け変更があれば箇条書きを更新
- **サンプルコード** — 新構文を使った例を追加・修正
- **言語仕様**（型・演算子・型規則・組み込み関数・変数宣言・制約） — 仕様変更があれば該当箇所を更新
- **必要環境 / ビルド / 実行** — 依存関係やビルド手順に変更があれば更新

反映が不要と判断した場合は、その理由を明示すること（内部リファクタリングのみ、テスト追加のみ、等）。

### 2. 全テスト実行

すべての変更が完了した後、全テストを実行して成功を確認する。

```bash
cmake -B build -DLLVM_DIR=/usr/local/llvm/lib/cmake/llvm && cmake --build build && ./build/ry_tests
```

テストが失敗した場合は、原因を修正してから作業完了とすること。
